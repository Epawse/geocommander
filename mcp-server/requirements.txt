fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.26.0  # OpenAI 兼容 API 客户端

# MCP 支持
mcp>=1.0.0

# MCP Geo Tools (本地开发时使用 -e 安装)
# pip install -e ../mcp-geo-tools
# 或发布后: mcp-geo-tools>=0.1.0
